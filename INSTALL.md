# Medici Synapse Skill

将此 skill 添加到你的 Claude Code 环境中：

## 方法 1: 直接复制

1. 复制整个 `medici-synapse` 文件夹到你的 Claude skills 目录：
   ```
   ~/.claude/skills/medici-synapse/
   ```

2. 在 Claude Code 中运行：
   ```
   /medici-synapse
   ```

## 方法 2: 符号链接（推荐）

```bash
# 在你的 skills 目录中创建符号链接
ln -s /path/to/medici-synapse-skills/skills/medici-synapse ~/.claude/skills/medici-synapse
```

## 验证安装

在 Claude Code 中输入：

```
使用 medici-synapse skill
```

如果看到以下输出，说明安装成功：

```
# 🎭 The Medici Synapse - Ready

已启动跨域创新引擎。正在等待两个截然不同的领域输入...
```

## 开始使用

输入任意两个领域，例如：

```
火锅店 + 赛博朋克黑客技术
```

然后坐下来，准备好被颠覆！🚀
